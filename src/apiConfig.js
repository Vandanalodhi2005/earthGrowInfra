const PRODUCTION_BACKEND = 'https://backend-realtyexperts.vercel.app';

const getBackendUrl = () => {
  // For Next.js, we'll use relative paths since we have API routes set up
  return '';
};

export const API_URL = getBackendUrl();
